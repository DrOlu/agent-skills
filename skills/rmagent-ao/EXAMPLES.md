# rmagent-ao

Grain: `(host, agent, session)`. Cases: `~/.rmagent/cases/`.

```bash
python3 scripts/hunt.py --inventory estate.yaml --since 2h
```

Secret values never appear in answers (`sk-` / `Bearer` redacted).
Tier 4 agents are holes, not “no agents found”.
